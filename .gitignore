node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Local machine overrides (port conflicts, personal settings)
docker-compose.override.yml
.tmp-body
.env.prodtest

# Terraform: state holds the generated secrets in plain text, and .terraform
# is a few hundred MB of downloaded providers.
infra/.terraform/
infra/.terraform.lock.hcl
infra/*.tfstate
infra/*.tfstate.*
infra/*.tfvars
!infra/*.tfvars.example
infra/tfplan
.playwright-mcp/
*.png

# Kept locally, not published: build notes, agent conventions, and the
# employer-supplied brief.
CLAUDE.md
docs/plan.md
docs/*.docx
